import {
	init,
	nextTick,
	setAlive,
	clearBoard,
	startGame,
	pauseGame,
	randomizeBoard,
} from './game';
import { mouseDown, mouseUp } from './ui';

export default {
	init,
	nextTick,
	setAlive,
	mouseDown,
	mouseUp,
	clearBoard,
	startGame,
	pauseGame,
	randomizeBoard,
};
